# Omni-Local RAG Python dependencies
#
# Install through install.bat / install_cpu.bat / install_cuda.bat.
# llama-cpp-python is intentionally NOT listed here: install scripts install
# the local cp312 win_amd64 wheel:
#   llama_cpp_python-0.3.19-cp312-cp312-win_amd64.whl

# GUI
PyQt5==5.15.10

# Embeddings
sentence-transformers==2.7.0

# PDF parsing backends
docling==1.0.0
deepsearch-toolkit==1.0.1
hf_xet==1.4.3
PyMuPDF==1.24.0
unstructured[pdf]==0.15.7
marker-pdf==1.10.2

# MinerU 2.0.6 requires Pillow>=11, while docling/marker/surya require
# Pillow<11. install_cpu.bat/install_cuda.bat install MinerU into an isolated
# tools/mineru-venv and configure the app to call its mineru.exe.

# OCR fallback. Tesseract itself is an external Windows program; use
# install_tesseract.bat if it is not already installed.
pytesseract==0.3.10

# ASR
faster-whisper==1.0.3

# Audio waveform
librosa==0.10.2
soundfile==0.12.1

# Global hotkey
keyboard==0.13.5

# Packaging
pyinstaller==6.6.0

# Compatibility pins shared by docling / marker / unstructured / deepsearch.
numpy==1.26.4
Pillow==10.3.0
lxml==4.9.4
rich==13.9.4
onnxruntime==1.18.1
pypdfium2==4.30.0
